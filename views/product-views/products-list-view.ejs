<h2> Products List </h2>

<ul>
  <% productsAndStuff.forEach((product) => { %>
    <li>
      <h3> <%= product.name %> </h3>
      <p> $<%= product.price %> </p>
      <a href="/product/details?myId=<%= product._id %>">
        View details
      </a>
      <br>
    </li>
  <% }) %>
</ul>
